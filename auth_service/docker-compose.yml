version: '3.9'

services:

  auth_service:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: auth_service

    ports:
      - "8080:8080"

    env_file:
      - .env

    restart: unless-stopped